def generate_egl_entries(output, lines, i):
    for line in lines:
        if (line.find('EGL_ENTRY(') >= 0):
            line = line.split(',')[1].strip()
            output.write(('        %s = %d;\n' % (line, i)))
            i += 1
    return i
