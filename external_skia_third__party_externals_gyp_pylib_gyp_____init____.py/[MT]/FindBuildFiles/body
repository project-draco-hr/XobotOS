def FindBuildFiles():
    extension = '.gyp'
    files = os.listdir(os.getcwd())
    build_files = []
    for file in files:
        if (file[(- len(extension)):] == extension):
            build_files.append(file)
    return build_files
