{
  StringBuilder sb=new StringBuilder();
  sb.append(_name);
  sb.append('<');
  for (int i=0; i < _args.length; i++) {
    if (i > 0)     sb.append(',');
    sb.append(_args[i].getTypeName());
  }
  sb.append('>');
  return sb.toString();
}
